version: 2.1
jobs:
  publish_images:
    machine: true
    steps:
      - checkout
      - run: echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin
      - run: |
          cd gcloud
          docker build --pull -t vanmoof/gcloud:latest .
          docker push vanmoof/gcloud:latest
      - run: |
          cd pubsub-emulator
          docker build --pull -t vanmoof/pubsub-emulator:latest .
          docker push vanmoof/pubsub-emulator:latest


workflows:
  version: 2
  publish:
    jobs:
    - publish_images
  auto_publish:
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              only: master
    jobs:
      - publish_images
